def add_delete_link (object)
  <% if current_user?(object.user) %>
    <%= link_to "delete", object, method: :delete,
                                     data: { confirm: "You sure?" },
                                     title: object.content %>
  <% end %>
